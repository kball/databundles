# Configuration for a library that is backed by a remote library

library:
    default:
      filesystem: default
      database: default

warehouse:
    default:
      database: postgres_warehouse

    postgres:
      database: postgres_warehouse

    mysql:
      database: mysql_warehouse
 
    sqlite:
      database: sqlite_warehouse


database:
  
    default:
        dbname: '/tmp/server/library.db'
        driver: sqlite  
  
    postgres_warehouse:
        driver: postgres
        server: lorne
        dbname: test-library
        username: test
        password: test 
        
    mysql_warehouse:
        driver: mysql
        server: testdb.cnhz40f0cmdb.us-east-1.rds.amazonaws.com
        dbname: test
        username: test
        password: samizdat 

    sqlite_warehouse:
        dbname: '/tmp/server/warehouse.db'
        driver: sqlite  


filesystem:  
  root_dir: /tmp/warehouse-test/
  default:
      dir: '{root}/cache-l1'
      size: 50
      upstream:
        dir: '{root}/cache-l2'
        options: [compress]


        